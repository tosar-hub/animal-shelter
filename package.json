{
  "name": "animal-shelter",
  "version": "1.0.0",
  "scripts": {
    "build": "node fetch-sheets.js && npx @11ty/eleventy",
    "start": "npm run build -- --serve"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.5"
  }
}